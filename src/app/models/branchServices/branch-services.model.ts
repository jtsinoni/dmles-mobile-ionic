
import { BranchModel } from "./branch.model";
//import { SiteModel } from "./site.model";

export class BranchServicesModel extends Array<BranchModel> {

    // branches: Array<BranchModel>;

    // getSiteByDodaac(dodaac: string) : SiteModel {

    //     let site: SiteModel = null;
    //     for (let branch of this.branches) {
    //        site = branch.getSiteByDodaac(dodaac);
    //        if (site) {
    //            break;
    //        }
    //     }
    //     return site;
    // }

    // toStringArray() : Array<string> {
    //     let builder: Array<string> = new Array<string>();  
    //     for (let branch of this.branches) {
    //         builder.push(branch.toString());

    //     }

    //     return builder;
    // }
}